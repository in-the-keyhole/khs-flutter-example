{
  "name": "khs-flutter-example",
  "version": "0.1.0",
  "private": true,
  "description": "Flutter example app",
  "scripts": {
    "flutter:run": "cd . && flutter run",
    "flutter:build": "flutter build",
    "flutter:clean": "flutter clean",
    "flutter:pub": "flutter pub get"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
